{{- define "systemd-sysctl" -}}
{{/* it needs to be reloaded, because the /etc/containerd.service.d/ files are not present, when this is started for a first time */ -}}
- name: containerd.service
  command: restart
  enable: true
{{- end -}}
